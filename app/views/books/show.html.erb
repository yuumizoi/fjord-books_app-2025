<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('.edit'), edit_book_path(@book) %> |
  
  <%= link_to t('.back'), books_path %>

  <%= button_to t('.destroy'), @book, method: :delete %>
</div>
