<% content_for :title, t('views.common.title_new', name: Report.model_name.human) %>

<h1><%= t('views.common.title_new', name: Report.model_name.human) %></h1>

<%= render "form", report: @report %>

<br>

<div>
  <%= link_to t('views.common.back', name: Report.model_name.human), reports_path %>
</div>
